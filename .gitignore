node_modules/
dist/
dist-web/
generated-profiles/
workspaces/
conversations/
.jira-cache/
*.log
.env
.env.local
.DS_Store
*.tsbuildinfo

# Cấu hình Claude riêng theo máy (không đồng bộ) — memory ở .claude/memory/ thì vẫn commit
.claude/settings.local.json

# Agent Skills bow-agent tự TRẢI ra lúc runtime (nguồn ở skills/agent-skills/) — không track
.claude/skills/

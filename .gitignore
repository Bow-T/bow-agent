node_modules/
dist/
dist-web/
generated-profiles/
workspaces/
conversations/
.jira-cache/
*.log
.env
.env.local
.DS_Store
*.tsbuildinfo

# Cấu hình Claude riêng theo máy (không đồng bộ) — memory ở .claude/memory/ thì vẫn commit
.claude/settings.local.json

# Agent Skills bow-agent tự TRẢI lúc runtime (nguồn: repo GitHub bow-skill-core/<stack> clone
# về ~/.bow/skills-cache/, rồi trải sang đây) — không track
.claude/skills/
